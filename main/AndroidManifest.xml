<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.lauwba.wisatajogja">

    <!-- TAMBAH IZIN INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- start komen -->
        <activity android:name=".Home" />
        <activity android:name=".WebWisata" />
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:noHistory="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- incompatible @style.AppTheme.NoActionBar jadi diganti -->
        <activity android:name="android.support.multidex.MultiDexApplication"
            <meta-data android:name="com.google.android.geo.API_KEY" android:value="AIzaSyC_VLuZtZPAbTIPXwaVvOYz3TRq_30Of4s"/>

        <!-- end komen -->

        <activity android:name=".PetaYogyakarta"></activity>
    </application>

</manifest>